[project]
name = "liberox-local-studio-backend"
version = "0.6.0"
requires-python = ">=3.10,<3.11"
dependencies = [
  "fastapi==0.141.1",
  "uvicorn[standard]==0.52.1",
  "websockets==16.1",
  "PyYAML>=6,<7",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
